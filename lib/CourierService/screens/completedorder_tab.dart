import "package:flutter/material.dart";

class CourierCompletedOrderTab extends StatelessWidget {
  @override
  Widget build(BuildContext contx) {
    return Center(
      child: Text("Active orders"),
    );
  }
}
