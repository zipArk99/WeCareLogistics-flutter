import 'package:flutter/material.dart';
import 'package:wecare_logistics/drawer/drawer_screen.dart';

class PublishedOrderTab extends StatelessWidget {
  @override
  Widget build(BuildContext contx) {
    return Scaffold(
      body: Center(
        child: Text("Published orders"),
      ),
    );
  }
}
